@if (ProductService.products == null || ProductService.products.Count == 0)
{
    <span> Loading products...</span>
}
else
{
   
        <MudGrid>
            @foreach (var product in ProductService.products)
            {
                 var link = "/product/" + product.Id;

                <MudItem xs="12" sm="6" md="3">
                        <MudCard Class="d-flex flex-column relative overflow-hidden" Style="height:100%">
                            <MudImage ObjectFit="@ObjectFit.Fill"  ObjectPosition="ObjectPosition.Center" Class="zoomed" Height="250" Src=@product.ImageUrl />
                            <MudCardContent Style="height: 40%">
                                <MudLink Href="@link" Typo="Typo.h5">@product.Title</MudLink>
                                <MudText Typo="Typo.body2">@product.Description</MudText>
                            </MudCardContent>
                            <MudCardActions Class="relative object-bottom">
                                <MudText Color="Color.Success" Typo="Typo.h6">$@product.Price</MudText>
                            </MudCardActions>
                        </MudCard>
                </MudItem>

            }
        </MudGrid>

}
